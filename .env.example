# Configuração do Supabase
# Substitua pelos valores do seu projeto

# URL do seu projeto Supabase
# Encontre em: Dashboard Supabase → Settings → API → Project URL
VITE_SUPABASE_URL=https://SEU-PROJETO-ID.supabase.co

# Chave pública anônima (anon key) - NÃO é a service_role!
# Encontre em: Dashboard Supabase → Settings → API → anon public
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# IMPORTANTE:
# - Use APENAS a anon key (chave pública)
# - NUNCA commite a service_role key no código
# - O arquivo .env real não deve ir para o Git (está no .gitignore)